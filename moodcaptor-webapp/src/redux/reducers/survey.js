
const initialState = {
    groupId: "",
    moodLevel: 0,
    comment: ""
}

export default function (state = initialState, action) {
    switch (action.type) {
        default:
            return state
    }
}